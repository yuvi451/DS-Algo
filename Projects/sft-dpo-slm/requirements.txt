# Pinned per project breakdown (§2) — TRL's API has churned across minor
# versions (DPOTrainer/SFTTrainer config classes, tokenizer= -> processing_class=),
# so these versions are load-bearing, not decorative.
transformers==4.46.*
trl==0.12.*
peft==0.13.*
datasets==3.0.*
accelerate==1.0.*
bitsandbytes==0.44.*
torch>=2.4,<2.6
huggingface_hub>=0.25
sentencepiece
protobuf
openai>=1.40  # optional: only used if JUDGE_BACKEND="openai"
